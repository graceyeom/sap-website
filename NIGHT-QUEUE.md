# 야간 작업 큐

밤에 도는 자동 에이전트가 이 파일만 보고 움직인다.
`## 큐` 아래 맨 위 미완료 항목 **1개**만 집어가서 브랜치 1개 · PR 1개로 만든다.
큐가 비어 있으면 그날 밤은 아무 일도 하지 않는다 (정상 동작).

**적을 때 규칙**

- 한 문장으로 "이렇게 되면 완료"가 써지는 것만 적는다.
- 사람 판단·의사결정·문구 승인이 필요한 일은 적지 않는다.
- env 세팅, 배포, 실제 발송처럼 코드 밖에서 벌어지는 일은 적지 않는다.

**검증 명령**: `npm run build`

**주의**: `CLAUDE.md`에 따르면 Supabase는 **의도적으로 비활성** 상태다. 되살리는 작업은
큐에 넣지 말 것.

## 큐

<!-- 여기에 - [ ] 로 한 줄씩. 위에 있는 게 먼저 처리된다. -->

## 완료

<!-- 에이전트가 PR 링크와 함께 여기로 옮긴다 -->
